# /materials

Small files (PDFs, slide decks under ~20MB) referenced from `data/lectures-data.js`
or `data/handson-data.js` go here. Use GitHub's "Add file > Upload files" button
in the browser — no command line needed.

For anything larger, or for videos, use a Google Drive link instead (see the
instructions inside the data files).
